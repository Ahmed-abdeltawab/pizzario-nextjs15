# دليل عملي: اختبار ربط أخطاء الخادم

## 🧪 اختبار شامل للأخطاء

### السيناريو 1: أخطاء التحقق (Client-side Validation)

#### الاختبار:
```
1. اترك حقل "الاسم" فارغاً
2. اضغط "Save Changes"
```

#### النتيجة المتوقعة:
```
❌ Name field error: "Full name is required"
   (الخطأ يظهر فوراً قبل إرسال البيانات)
```

#### الكود المسؤول:
```typescript
// في validation/auth.ts (Zod Schema)
name: z.string().min(1, "Full name is required")
```

---

### السيناريو 2: أخطاء الخادم (Server-side Validation)

#### الاختبار:
```
1. غيّر البريد الإلكتروني إلى بريد موجود بالفعل
   (مثلاً: اطلب من مستخدم آخر أن يُسجل بنفس البريد)
2. اضغط "Save Changes"
```

#### النتيجة المتوقعة:
```
⏳ Button shows "Saving..."
   (ينتظر رد الخادم)
   
❌ Email field error: "Email is already in use"
   (الخطأ يظهر تحت حقل البريد)
```

#### الكود المسؤول:
```typescript
// في src/app/actions/profile.ts
const existingEmail = await prisma.user.findUnique({
  where: { email: updateData.email },
});

if (existingEmail && existingEmail.id !== userId) {
  serverErrors.email = ["Email is already in use"];
}

return {
  success: false,
  message: "Please fix the errors below.",
  errors: serverErrors,
};
```

---

### السيناريو 3: النجاح والتحديث

#### الاختبار:
```
1. أدخل بيانات صحيحة جديدة
2. اضغط "Save Changes"
```

#### النتيجة المتوقعة:
```
⏳ Button shows "Saving..." لثوانٍ
   (انتظار الخادم)
   
✅ Saving complete
   (البيانات محفوظة)
   
✅ Form resets with new values
   (يعرض البيانات الجديدة)
   
✅ No error messages
   (جميع الأخطاء محسوبة)
   
✅ Session updated
   (البيانات في ذاكرة الجلسة محدثة)
```

---

## 🔍 كيفية تتبع الأخطاء

### في متصفح الويب:

#### 1. افتح Developer Tools
```
F12 أو Right Click → Inspect
```

#### 2. اذهب إلى Console
```
Console → تفقّد السجلات
```

#### 3. ابحث عن الرسائل:
```
console.log في الكود:
- "UPDATE_PROFILE_ERROR: ..." (خطأ)
- "formDataObject: ..." (البيانات المُرسلة)
```

#### 4. اذهب إلى Network
```
Network → اضغط Save → شاهد الطلب
- Status 200: نجح ✅
- Status 400/500: فشل ❌
```

---

## 🧠 فهم تدفق الأخطاء

```
المستخدم يُدخل البيانات
        │
        ▼
  ┌─────────────┐
  │ Client-side │
  │ Validation  │
  │ (Zod)       │
  └──────┬──────┘
         │
         ├─ ❌ خطأ → عرض فوراً (لا يرسل)
         │
         └─ ✅ صحيح → أرسل للخادم
                │
                ▼
           ┌──────────────┐
           │ Server-side  │
           │ Validation   │
           │ (Business    │
           │  Logic)      │
           └──────┬───────┘
                  │
         ┌────────┴────────┐
         │                 │
         ▼                 ▼
      ❌ خطأ            ✅ صحيح
    (أرجع errors)    (حفظ البيانات)
         │                 │
         └─────────┬───────┘
                   │
                   ▼
          ┌────────────────┐
          │ Component      │
          │ useEffect      │
          │ (معالجة الأخطاء)│
          └────────┬───────┘
                   │
         ┌─────────┴─────────┐
         │                   │
         ▼                   ▼
    setError()        clearErrors()
    (ربط الأخطاء)      (مسح الأخطاء)
         │                   │
         └─────────┬─────────┘
                   │
                   ▼
         ┌──────────────────┐
         │ FormInputField   │
         │ يعرض الخطأ       │
         └──────────────────┘
                   │
                   ▼
         ❌ "Email is already in use"
         (تحت حقل البريد)
```

---

## 📝 قائمة الاختبار

### ✅ اختبارات أساسية

- [ ] **اختبار 1: حقول مطلوبة فارغة**
  ```
  ترك الاسم فارغاً → يجب أن يظهر خطأ
  ```

- [ ] **اختبار 2: صيغة بريد غير صحيحة**
  ```
  إدخال "notanemail" → يجب أن يظهر خطأ
  ```

- [ ] **اختبار 3: بريد موجود بالفعل**
  ```
  استخدام بريد مستخدم آخر → خطأ من الخادم
  ```

- [ ] **اختبار 4: حفظ ناجح**
  ```
  إدخال بيانات صحيحة → يجب أن يحفظ بنجاح
  ```

### 🔄 اختبارات متقدمة

- [ ] **اختبار 5: مسح الأخطاء عند النجاح**
  ```
  بعد النجاح → جميع الأخطاء تختفي
  ```

- [ ] **اختبار 6: إعادة تعيين النموذج**
  ```
  بعد النجاح → النموذج يعيد القيم الجديدة
  ```

- [ ] **اختبار 7: تحديث الجلسة**
  ```
  بعد النجاح → بيانات الجلسة تتحدث
  ```

- [ ] **اختبار 8: حقل قراءة فقط**
  ```
  حقل البريد → لا يمكن تعديله (readOnly)
  ```

---

## 🐛 تصحيح الأخطاء الشائعة

### المشكلة: الأخطاء لا تظهر

```typescript
❌ التشخيص:
1. فتح Developer Tools
2. اذهب إلى Network
3. تحقق من الاستجابة: status 200 مع errors في الـ payload

🔧 الحل:
- تأكد من أن setError يُستدعى في useEffect
- تأكد من أن state.errors يحتوي على البيانات
```

### المشكلة: الأخطاء لا تختفي

```typescript
❌ التشخيص:
1. نجح الحفظ لكن الأخطاء تبقى
2. زر الحفظ لا يزال يظهر تحميل

🔧 الحل:
- تأكد من استدعاء clearErrors()
- تأكد من أن state.success يتغيّر
```

### المشكلة: البيانات لا تتحدث

```typescript
❌ التشخيص:
1. الحفظ نجح لكن الفورم يعيد القيم القديمة

🔧 الحل:
- تأكد من استدعاء reset()
- تأكد من أن session.user يحتوي على البيانات الجديدة
```

---

## 📊 مثال حقيقي: اختبار خطأ البريد

### الخطوات:

1. **افتح الملف: `prisma/seed.ts` أو قاعدة البيانات**
   ```
   أنشئ حسابين:
   - user1@example.com (محفوظ بالفعل)
   - user2@example.com (سجّل دخول به)
   ```

2. **في صفحة الملف الشخصي:**
   ```
   اضغط على حقل البريد
   اكتب: user1@example.com
   اضغط: Save Changes
   ```

3. **توقع النتيجة:**
   ```
   ⏳ الزر يعرض "Saving..."
   ❌ رسالة خطأ: "Email is already in use"
   ✅ تحت حقل البريد مباشرة
   ```

4. **تصحيح الخطأ:**
   ```
   اكتب: user2_new@example.com
   اضغط: Save Changes
   ```

5. **النجاح:**
   ```
   ✅ "Saving..." ثم انتهى
   ✅ الفورم اختفت أخطاؤه
   ✅ البريد تحدث في الفورم والجلسة
   ```

---

## 🎯 النقاط المهمة

### ✅ ما يجب أن يحدث:

1. **أخطاء التحقق تظهر فوراً** (قبل الإرسال)
2. **الزر يظهر "Saving..." أثناء الانتظار** (حالة تحميل)
3. **أخطاء الخادم تظهر تحت الحقول** (بعد الرد)
4. **الأخطاء تختفي عند النجاح** (تنظيف)
5. **البيانات تتحدث في الفورم والجلسة** (مزامنة)

---

## 💡 نصائح للاختبار

### 1. استخدم عدة متصفحات
```
Chrome, Firefox, Safari
تأكد من أن الأخطاء تظهر في الجميع
```

### 2. جرّب على أجهزة مختلفة
```
Desktop, Tablet, Mobile
تأكد من أن الأخطاء مرئية
```

### 3. اختبر التعطّل المتعمد
```
أطفئ الإنترنت أثناء الحفظ
تأكد من ظهور رسالة خطأ
```

### 4. استخدم أدوات المطور
```
Network tab: شاهد الطلب والاستجابة
Console: شاهد السجلات والأخطاء
Elements: افحص DOM للأخطاء
```

---

## 🎊 ملخص الاختبار

```
✅ اختبارات أساسية (5 دقائق)
├─ أخطاء التحقق تظهر
├─ الزر يعمل
├─ أخطاء الخادم تظهر
├─ البيانات تحفظ
└─ الفورم ينظّف

✅ اختبارات متقدمة (10 دقائق)
├─ مسح الأخطاء ✓
├─ تحديث الجلسة ✓
├─ إعادة التعيين ✓
└─ الحالات الحدية ✓

✅ اختبارات الأجهزة (5 دقائق)
├─ Desktop ✓
├─ Tablet ✓
├─ Mobile ✓
└─ سرعات اتصال مختلفة ✓
```

---

## 📚 مرجع سريع

```typescript
// الخطأ يظهر تحت الحقل:
errors.phone?.message

// الخطأ يأتي من:
state.errors (من الخادم)

// يُربط بـ:
setError() في useEffect

// ويُمسح مع:
clearErrors() عند النجاح

// ويعاد تعيينه مع:
reset() للبيانات الجديدة
```
